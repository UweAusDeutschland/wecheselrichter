# docker-compose.yml - Sungrow Inverter Monitor mit Flask Webinterface
services:
  sungrow-monitor:
    build: .
    volumes:
      - ./data:/app/data  # Daten persistent speichern
    ports:
      - "8080:8080"
    restart: unless-stopped